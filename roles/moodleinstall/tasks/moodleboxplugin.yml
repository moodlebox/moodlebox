---
- name: Get MoodleBox plugin from git repository
  ansible.builtin.git:
    repo: 'https://github.com/moodlebox/moodle-tool_moodlebox.git'
    dest: '{{ moodlebox_moodle_source_dir }}/public/admin/tool/moodlebox'
    version: '{{ moodlebox_moodlebox_plugin_version }}'
    depth: 1

- name: Change MoodleBox plugin file permissions (1)
  ansible.builtin.file:
    path: '{{ moodlebox_moodle_source_dir }}/public/admin/tool/moodlebox'
    state: 'directory'
    owner: 'www-data'
    group: '{{ moodlebox_username }}'
    mode: 'ug+w,o-w'
    recurse: true

- name: Change MoodleBox plugin file permissions (2)
  ansible.builtin.file:
    path: '{{ moodlebox_moodle_source_dir }}/public/admin/tool/moodlebox/bin'
    state: 'directory'
    mode: '0774'
    recurse: true

- name: Create directory for MoodleBox plugin settings files
  ansible.builtin.file:
    path: '{{ moodlebox_moodle_data_dir }}/moodlebox'
    state: 'directory'
    owner: 'www-data'
    group: '{{ moodlebox_username }}'
    mode: '0775'

- name: Touch settings files for MoodleBox plugin functionality
  ansible.builtin.file:
    path: '{{ moodlebox_moodle_data_dir }}/moodlebox/{{ item }}'
    state: 'touch'
    owner: 'www-data'
    group: '{{ moodlebox_username }}'
    mode: '0664'
  with_items:
    - '.reboot-server'
    - '.shutdown-server'
    - '.set-server-datetime'
    - '.newpassword'
    - '.wifisettings'
    - '.resize-partition'

- name: Configure direvent
  ansible.builtin.template:
    src: 'etc/direvent.conf.j2'
    dest: '/etc/direvent.conf'
    mode: '0644'
  register: moodleinstall__direvent_configured

- name: Create sudoers file for MoodleBox plugin usage
  ansible.builtin.copy:
    src: 'etc/sudoers.d/020_www-data-nopasswd'
    dest: '/etc/sudoers.d/020_www-data-nopasswd'
    mode: '0440'
    validate: 'visudo -cf %s'

- name: Install MoodleBox plugin via CLI
  ansible.builtin.command: '/usr/bin/php "{{ moodlebox_moodle_source_dir }}/admin/cli/upgrade.php"
    --allow-unstable
    --non-interactive'
  args:
    chdir: '{{ moodlebox_moodle_source_dir }}'
  register: moodleinstall_moodlebox_plugin_installed
  changed_when: moodleinstall_moodlebox_plugin_installed.rc == 0
