---
- name: restart direvent
  systemd:
    name: 'direvent'
    state: 'restarted'

- name: restart web services
  systemd:
    name: '{{ item }}'
    state: 'restarted'
    daemon_reload: 'yes'
  with_items:
    - 'nginx'
    - 'php7.4-fpm'
